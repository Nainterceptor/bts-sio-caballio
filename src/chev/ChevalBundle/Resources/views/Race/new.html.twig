<h1>Race creation</h1>

<form action="{{ path('cheval_race_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('cheval_race') }}">
            Back to the list
        </a>
    </li>
</ul>
