<?php

namespace chev\PensionBundle\Entity;

use Doctrine\ORM\EntityRepository;

/**
 * PaiementRepository
 *
 * This class was generated by the Doctrine ORM. Add your own custom
 * repository methods below.
 */
class PaiementRepository extends EntityRepository
{
	/**
     * Trouver les paiements par le gérant
     * 
     * @param User $gerant Le gérant
     * 
     * @return Entity
     */
	public function findByGerant($gerant) {
		return $this->_em
                ->createQuery('SELECT p FROM chevPensionBundle:Paiement p
                               JOIN p.facture f
                               JOIN f.box b
                               JOIN b.type t
                               JOIN t.centre c
                               WHERE c.gerant = :gerant')
                ->setParameter(':gerant', $gerant)
                ->getResult();
	}
	/**
     * Trouver un paiement par l'id et le gérant
     * 
     * @param User $gerant Le gérant
     * @param int $id l'id
     * 
     * @return Entity
     */
	public function findOneByGerant($gerant, $id) {
		return $this->_em
                ->createQuery('SELECT p FROM chevPensionBundle:Paiement p
                               JOIN p.facture f
                               JOIN f.box b
                               JOIN b.type t
                               JOIN t.centre c
                               WHERE c.gerant = :gerant
                               AND p.id = :id')
                ->setParameter(':gerant', $gerant)
				->setParameter(':id', $id)
                ->getResult();
	}
}
