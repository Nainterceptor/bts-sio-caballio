<h1>TypePaiement creation</h1>

<form action="{{ path('paiement_type_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('paiement_type') }}">
            Back to the list
        </a>
    </li>
</ul>
