{% extends 'chevBoxBundle:Box:layout.html.twig' %}
{% block breadcrumbs %}
    {{ parent() }}
    <li><a href="{{ path('centre_typebox_new')}}">New</a></li>
{% endblock %}
{% block body %}
<h2>TypeBox creation</h2>

<form action="{{ path('centre_typebox_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('centre_typebox') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}