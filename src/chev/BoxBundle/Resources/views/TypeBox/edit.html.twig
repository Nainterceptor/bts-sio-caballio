{% extends 'chevBoxBundle:Box:layout.html.twig' %}
{% block breadcrumbs %}
    {{ parent() }}
    <li><a href="{{ path('centre_typebox_edit', {id: entity.id}	)}}">Modifier</a></li>
{% endblock %}
{% block body %}
<h2>TypeBox edit</h2>

<form action="{{ path('centre_typebox_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Modifier</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('centre_typebox') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('centre_typebox_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Supprimer</button>
        </form>
    </li>
</ul>
{% endblock %}