{% extends 'chevChevalBundle:Race:layout.html.twig' %}
{% block title %}Modifier - {{ parent() }}{% endblock %}
{% block breadcrumbs %}
{{ parent() }}
<li class="current">
     <a href="{{ path('cheval_race_edit', { 'id': entity.id }) }}">Modifier</a>
</li>
{% endblock %}
{% block body %}
<div class="menu">
	<a href="{{ path('cheval_race') }}">
		<img style="float right" src="{{ asset('images/annuler_32x32.png') }}">
	</a>
	<a href="{{ path('cheval_race_show', { 'id': entity.id }) }}">
		<img style="float right" src="{{ asset('images/voir_32x32.png') }}">
	</a>
    <form onsubmit="return confirm('{{ 'user.delete.confirm'|trans([], 'backoffice') }}')" action="{{ path('cheval_race_delete', { 'id': entity.id }) }}" method="post" class="delete image form">
        {{ form_widget(delete_form) }}
        <input type="image" src="{{ asset('images/supprimer_32x32.png') }}" style="float right" />
    </form>
</div>
<form action="{{ path('cheval_race_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
	<fieldset>
        <h2>Modifier race</h2>
       {{ form_rest(edit_form) }}
		<input class="float right" type="submit" value="Modifier">
    	<a title="Retourner à la liste" href="{{ path('cheval') }}">
    		<img class="float right" src="{{ asset('images/annuler_32x32.png') }}" alt="Retourner à la liste">
    	</a>
	</fieldset>
</form>
{% endblock %}