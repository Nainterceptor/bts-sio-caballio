{% extends 'chevPensionBundle:Paiement:layout.html.twig' %}
{% block title %}Voir - {{ parent() }}{% endblock %}
{% block breadcrumbs %}
{{ parent() }}
<li class="current">
     <a href="{{ path('paiement_show', {id: entity.id}) }}">Voir</a>
</li>
{% endblock %}
{% block body %}
<div class="menu">
	<a href="{{ path('paiement') }}">
		<img style="float right" src="{{ asset('images/annuler_32x32.png') }}">
	</a>
	<a href="{{ path('paiement_edit', { 'id': entity.id }) }}">
		<img style="float right" src="{{ asset('images/editer_32x32.png') }}">
	</a>
    <form onsubmit="return confirm('{{ 'user.delete.confirm'|trans([], 'backoffice') }}')" action="{{ path('paiement_delete', { 'id': entity.id }) }}" method="post" class="delete image form">
        {{ form_widget(delete_form) }}
        <input type="image" src="{{ asset('images/supprimer_32x32.png') }}" style="float right" />
    </form>
</div>
<h2>Voir le paiement</h2>
<table width="100%" cellspacing="0" cellpadding="0" class="item-list clear">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Date paiement</th>
            <td>{{ entity.datePaiement|date('d/m/Y H:i:s') }}</td>
        </tr>
        <tr>
            <th>Date encaissement</th>
            <td>{{ entity.dateEncaissement|date('d/m/Y H:i:s') }}</td>
        </tr>
        <tr>
            <th>Montant</th>
            <td>{{ entity.montant }} €</td>
        </tr>
    </tbody>
</table>

{% endblock %}