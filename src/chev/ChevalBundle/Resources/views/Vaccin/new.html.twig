<h1>Vaccin creation</h1>

<form action="{{ path('cheval_vaccin_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('cheval_vaccin') }}">
            Back to the list
        </a>
    </li>
</ul>
